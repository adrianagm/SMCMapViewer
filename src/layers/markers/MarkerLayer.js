/**
 * Base layer for all SMC map viewer's layers rendered using markers.
 * @class
 * @abstract 
 */
SMC.layers.markers.MarkerLayer = L.Class.extend(
/** @lends SMC.layers.markers.MarkerLayer# */
{
	
});